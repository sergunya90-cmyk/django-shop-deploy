# Практическая работа 7

### Цели практической работы
 - Самостоятельно применить в работе изученные теги и стили.
 - Создать веб-страницу в соответствии с макетом дизайна.

### Что нужно сделать
Закончите вёрстку страницы, которой занимались при прохождении уроков. Она должна примерно соответствовать макету дизайна.
1. На всей странице поменяйте шрифт на Roboto. Для этого вставьте в head страницы следующий код:    <link rel="preconnect" href="https://fonts.googleapis.com">
    ```html
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
    ```
   Код подключает к странице внешний шрифт с сервиса Google Fonts. Чтобы он появился во всех элементах, пропишите в стилях страницы:
      ```html
      * { font-family: 'Roboto', sans-serif;  }
      ```

   Стиль применит шрифт Roboto ко всем HTML-элементам.

2. Приведите ссылки меню в хедере и футере страницы к стилю, который похож на стиль в макете. Сделайте чёрный шрифт без подчёркивания. Подчёркивание должно появляться при наведении курсора на ссылки.
Выполнить задачу поможет видео урок «Форматирование текста» этого модуля. В нём разбирается аналогичный пример.
В хедере добавьте между ссылками отступы с помощью CSS-свойства margin-right. В футере — расположите их друг под другом и поместите в отдельный div-элемент. Задайте ему свойства технологии flexbox.
3. Увеличьте высоту первого блока так, чтобы фоновое изображение справа увеличилось, а его размер примерно соответствовал значениям в макете.
4. Сделайте так, чтобы внешние отступы у заголовка, подзаголовка и кнопки в первом блоке соответствовали значениям в макете. Для этого используйте CSS-свойство margin.
5. Увеличьте шрифт заголовков h1 и h2 так, чтобы он примерно соответствовал размеру шрифтов на макете.
6. Сверстайте блок с примерами работ по аналогии с блоком услуг:
    - создайте три элемента и задайте им размеры;
    - разместите их рядом друг с другом, используя flexbox;
    - в элементах разместите изображения.
7. Приведите в соответствие с макетом синий блок:
    - добавьте мелкий текст под заголовком в теге p;
    - установите ширину h2 и p 50%.
8. Приведите в соответствие с макетом футер страницы. Для этого рядом с блоком меню создайте ещё два: контакты и ссылки на социальные сети.
9. Разместите блоки и элементы в них так, как указано в макете. Используйте внешние отступы СSS-свойство margin и технологию flexbox.
10. Во всплывающем окне создайте поля «Имя», Email и кнопку «Отправить». Подписи к полям создавать не нужно. В поля впишите их названия при помощи атрибута placeholder. Пример:
    ```html
    <input type="text" placeholder="Введите ваше имя">
    ```
11. Сделайте так, чтобы всплывающее окно закрывалось при нажатии на внешнюю область или на кнопку «Отправить». Для этого в код JavaScript впишите следующее:
    ```javascript
    container.addEventListener('click', function (event) {
        if (event.target === container) {
            container.style.display = 'none';
        }
    });
    ```
12. В итоге код скрипта должен получиться таким:
    ```javascript
    document.addEventListener("DOMContentLoaded", function () {
        var container = document.querySelector('.popup-container');
        var popupButtons = document.querySelectorAll('.open-popup');
    
        for (let i = 0; i < popupButtons.length; i++) {
            popupButtons[i].addEventListener('click', function () {
                container.style.display = 'flex';
            });
        }
    
        container.addEventListener('click', function (event) {
            if (event.target === container) {
                container.style.display = 'none';
            }
        });
    });
    ```
13. Проверьте код страницы в [валидаторе HTML-документов](https://validator.w3.org/) и исправьте ошибки, следуя его указаниям и пояснениям.

#### Материалы для выполнения этого задания находятся [в папке](https://drive.google.com/drive/folders/1qNay3frbIS1jb3Uaeisw_NB5S-FZr0WD).


### Советы и рекомендации
- Если вам трудно выполнять задания, то посмотрите видеоурок, в котором разбиралась эта тема. 
- Для блоков div-элементов для футера рекомендуем использовать следующую структуру:
<img src="https://api.selcdn.ru/v1/SEL_72086/prodLMS/files/share/1637842173571.png">

### Критерии оценки
#### Зачёт:
- страница похожа на [итоговый макет](https://drive.google.com/file/d/1RsQiKyMbNVZqmZATBuyVBBKyW2OMWNTM/view?usp=sharing) при ширине экрана более 1200 px, полное соответствие по пикселям не требуется;
- в HTML-коде отсутствуют ошибки после [проверки валидатором.](https://validator.w3.org/)

#### На доработку:
- выполнены не все пункты задания, 
- страница сильно отличается от макета или есть ошибки в коде.

### Как отправить работу на проверку
Отправьте архив с веб-страницей и дополнительными файлами проверяющему преподавателю в LMS или разместите их в публичном доступе на любом хостинговом сервисе. Например, можно использовать TimeWeb.
